title: Ruflo MCP bridge -- T1190 unauthenticated tools/call to terminal_execute
id: 73fa19fa-dda1-497e-8ce0-3bba528a3651
status: experimental
description: |
  Vendor-disclosed detection for CVE-2026-59726 (GHSA-c4hm-4h84-2cf3,
  CVSS 10.0 CRITICAL), a self-reported advisory from Ruflo -- an agent
  meta-harness for Claude Code and Codex.

  Prior to 3.16.3, Ruflo's default `docker-compose` deployment bound the
  MCP bridge to all network interfaces (not loopback-only) and exposed
  `POST /mcp` and `POST /mcp/:group` without authentication. The advisory
  states the tool blocklist that was supposed to cover `terminal_execute`
  "was enforced only in the autopilot flow" -- these two HTTP endpoints
  bypassed it entirely, so any unauthenticated network caller could invoke
  `tools/call` against `terminal_execute`, obtain a shell in the bridge
  container as the `node` user, read provider API keys from the container
  environment, and poison the AgentDB learning-store with attacker-chosen
  patterns.

  Detection logic flags a JSON-RPC `tools/call` request whose `params.name`
  is `terminal_execute`, arriving over HTTP without the bearer-token header
  the fix later requires -- the MCP protocol's tool-invocation envelope is
  the discriminator, not a generic "suspicious HTTP POST" heuristic. This is
  the first rule in this corpus targeting the MCP protocol's own request
  shape rather than a downstream OS-level effect, since this plugin is
  itself an MCP server and the technique is directly relevant to its own
  threat model.

  Aggregated from 1 first-party vendor security advisory (Ruflo, via GHSA
  and NVD), fixed in 3.16.3.
references:
- https://github.com/ruvnet/ruflo/security/advisories/GHSA-c4hm-4h84-2cf3
- https://github.com/ruvnet/ruflo/pull/2521
- https://github.com/ruvnet/ruflo/releases/tag/v3.16.3
- https://attack.mitre.org/techniques/T1190/
author: WinstonRedGuard -- sigma plugin observed rules (derived from breach corpus)
date: '2026-08-11'
logsource:
  category: proxy
  product: ""
detection:
  selection_mcp_endpoint:
    c-uri|contains:
    - /mcp
    - /mcp/
  selection_method_post:
    cs-method: POST
  selection_terminal_execute_call:
    cs-uri-query|contains: terminal_execute
  selection_no_auth_header:
    cs(Authorization): null
  condition: selection_mcp_endpoint and selection_method_post and selection_terminal_execute_call and selection_no_auth_header
falsepositives:
- A deployment that has since upgraded to 3.16.3+ (which requires
  `MCP_AUTH_TOKEN`) but whose proxy/WAF log format still shows the request
  line before the auth-gate rejects it -- correlate with the response
  status code (a fixed deployment answers 401/403, not 200) to distinguish
  a blocked probe from a successful exploitation
- Legitimate internal automation calling `terminal_execute` through a
  correctly-configured bearer-token header -- the missing-Authorization
  condition is the discriminator; a log source that does not capture
  request headers cannot safely deploy this rule as-is
- Pattern library v1 -- review for environment-specific tuning before
  deployment
level: critical
tags:
- attack.t1190
- wrg.observed.cve.cve_2026_59726
- wrg.observed.source.ruflo_ghsa_c4hm_4h84_2cf3
- wrg.observed.vector.mcp_protocol_unauth_tool_call
- wrg.observed.technique.mcp_server_own_threat_model
- wrg.severity.critical
- wrg.observed
- wrg.tactic.initial_access