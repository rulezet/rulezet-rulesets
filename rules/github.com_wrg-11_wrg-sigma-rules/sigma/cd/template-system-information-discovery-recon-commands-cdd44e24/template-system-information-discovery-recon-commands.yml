title: Template -- System Information Discovery (recon commands)
id: 29633113-b612-50b4-9de8-770aaad349bd
status: experimental
description: Canonical sigma detection template for MITRE ATT&CK T1082 (System Information Discovery (recon commands)). Synthetic
  exemplar from the WinstonRedGuard threat-intel corpus -- no actor binding; review and bind to your environment before deployment.
references:
- https://attack.mitre.org/techniques/T1082/
author: WinstonRedGuard -- sigma plugin canonical templates
date: '2026-05-21'
logsource:
  category: process_creation
  product: windows
detection:
  selection_sysinfo_command:
    Image|endswith:
    - \cmd.exe
    - \powershell.exe
    - \pwsh.exe
    CommandLine|contains:
    - systeminfo
    - wmic os get
    - Get-ComputerInfo
    - hostname &&
    - ver && wmic
  condition: selection_sysinfo_command
falsepositives:
- Inventory and asset-management agents running systeminfo or Get-ComputerInfo on a schedule from a service context (SCCM and Intune discovery, monitoring collectors)
- Helpdesk and onboarding scripts collecting the same output when a ticket is opened
- Administrators checking a host by hand, which the command line alone cannot distinguish from recon -- the parent process and session type carry that signal
level: medium
tags:
- attack.t1082
- wrg.template
- wrg.tactic.discovery