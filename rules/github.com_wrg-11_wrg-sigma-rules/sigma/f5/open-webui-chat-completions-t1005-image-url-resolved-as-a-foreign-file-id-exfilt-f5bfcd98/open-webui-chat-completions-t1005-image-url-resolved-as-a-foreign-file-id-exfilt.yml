title: Open WebUI chat completions -- T1005 image_url resolved as a foreign file id, exfiltrated via LLM OCR
id: ebfea9e7-5c66-43b5-911e-60ad247335b4
status: experimental
description: |
  Vendor-disclosed detection for CVE-2026-54009 (GHSA-wch8-mhj5-9frg,
  CVSS 6.5 MEDIUM), affecting Open WebUI prior to 0.9.6.

  `POST /api/chat/completions` accepts an `image_url.url` value that,
  when it does NOT start with `http://`, `https://`, or `data:image/`,
  is interpreted as a raw FILE ID and resolved against the global file
  table -- with NO OWNERSHIP CHECK. An authenticated user can set
  `image_url.url` to another user's file id; the server reads that
  file from disk, base64-encodes it, and injects it into the LLM
  request as if it were a legitimately-supplied image. The attacker
  then simply prompts the model to describe or OCR the "image" and
  reads the victim's private file content back through the model's own
  response -- an indirect, LLM-mediated exfiltration channel for a file
  the attacker never had legitimate access to.

  Detection logic flags a chat-completions request whose `image_url.url`
  field is a bare identifier (matching a file-id shape rather than an
  `http(s)://` or `data:image/` URI) that does not belong to the
  requesting user -- the exact ambiguity the vulnerable code exploited
  by treating "not a URL" as "must be my own file id".

  Aggregated from 1 first-party vendor security advisory (open-webui, via
  GHSA and NVD), fixed in 0.9.6.
references:
- https://github.com/open-webui/open-webui/security/advisories/GHSA-wch8-mhj5-9frg
- https://attack.mitre.org/techniques/T1005/
author: WinstonRedGuard -- sigma plugin observed rules (derived from breach corpus)
date: '2026-08-11'
logsource:
  category: application
  product: open_webui
detection:
  selection_chat_completions_context:
    Message|contains: image_url
  selection_non_url_value:
    Message|re: '"url":"(?!https?://|data:image/)[a-zA-Z0-9-]+"'
  condition: selection_chat_completions_context and selection_non_url_value
falsepositives:
- A deployment already upgraded to 0.9.6+, where the file-id resolution
  path validates ownership before reading the file -- a matching
  request on a fixed deployment targeting a foreign file id would be
  rejected
- A legitimate use of `image_url.url` pointing to the caller's OWN file
  -- this is the supported, intended use of the feature; this rule
  cannot distinguish that from a foreign-file attempt by request shape
  alone, since both submit a bare file-id-shaped string; correlate the
  referenced file's actual owner against the requesting user for
  confirmation
- Pattern library v1 -- review for environment-specific tuning before
  deployment; this rule requires application-level logging of the
  actual request body content, which most infrastructure log sources
  do not capture by default
level: medium
tags:
- attack.t1005
- wrg.observed.cve.cve_2026_54009
- wrg.observed.source.open_webui_ghsa_wch8_mhj5_9frg
- wrg.observed.vector.ambiguous_url_vs_file_id_parsing
- wrg.observed.vector.llm_mediated_exfiltration
- wrg.severity.medium
- wrg.observed
- wrg.tactic.collection