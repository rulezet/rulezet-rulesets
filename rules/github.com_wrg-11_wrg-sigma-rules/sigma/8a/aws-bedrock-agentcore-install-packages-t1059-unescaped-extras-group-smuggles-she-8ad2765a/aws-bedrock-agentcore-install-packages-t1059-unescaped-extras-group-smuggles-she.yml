title: AWS Bedrock AgentCore install_packages -- T1059 unescaped extras group smuggles shell metacharacters into a pip install command
id: 9102b324-3546-4f78-a182-93041b2c3d4e
status: experimental
description: |
  Vendor-disclosed detection for CVE-2026-16796 (GHSA-j6g5-3hh3-pgw8),
  affecting the AWS Bedrock AgentCore Python SDK (`bedrock-agentcore`)
  Code Interpreter client prior to 1.18.1.

  `install_packages()` in `code_interpreter_client.py` validated each
  package specifier against `VALID_PACKAGE_NAME`, but the extras-group
  portion of that regex was `(\[.*\])?` -- any characters at all inside
  the brackets. A specifier like `requests[$(id)]` or
  `` flask[`whoami`] `` passed validation, and because the vulnerable
  code joined the accepted specifiers with plain `" ".join(packages)`
  (no shell quoting) before building `pip install <specifiers>` and
  handing it to `executeCommand`, the bracket content reached a shell
  intact -- command substitution inside the extras group executes
  arbitrary commands inside the Code Interpreter sandbox. The fix
  tightened the extras-group pattern to `[a-zA-Z0-9._,\-]*` (valid pip
  extra names only) and added `shlex.quote()` around every specifier
  before joining.

  Detection logic flags an `install_packages` / `executeCommand`
  invocation whose package specifier's extras-group brackets contain a
  shell metacharacter -- `$(`, a backtick, a semicolon, or a bare space
  -- the exact injection shape the fix's regression tests
  (`requests[$(id)]`, `` flask[`whoami`] ``, `pandas[a b c]`) name.

  Aggregated from 1 first-party vendor security advisory (AWS, via
  GHSA and NVD) plus the actual pre/post-fix source diff (commit
  `3c4b4ee6`, PR #581) confirming the exact vulnerable regex and the
  missing `shlex.quote()`, fixed in 1.18.1.
references:
- https://github.com/aws/bedrock-agentcore-sdk-python/security/advisories/GHSA-j6g5-3hh3-pgw8
- https://github.com/aws/bedrock-agentcore-sdk-python/commit/3c4b4ee6
- https://attack.mitre.org/techniques/T1059/
author: WinstonRedGuard -- sigma plugin observed rules (derived from breach corpus)
date: '2026-08-11'
logsource:
  category: application
  product: ""
detection:
  selection_install_packages_command:
    Message|contains: 'pip install '
  selection_metachar_inside_extras_brackets:
    Message|re: '\[[^\]]*(\$\(|`|;|\s)[^\]]*\]'
  condition: selection_install_packages_command and selection_metachar_inside_extras_brackets
falsepositives:
- A deployment already upgraded to bedrock-agentcore 1.18.1+, where the
  extras-group pattern accepts only valid pip extra names and every
  specifier is shell-quoted before joining -- a matching crafted
  specifier on a fixed SDK version is rejected with "Invalid package
  name" rather than executed
- A legitimate multi-extra package specifier such as
  `celery[redis,auth]` that happens to trip a loosely-written version of
  this signature -- the pattern here specifically requires a shell
  metacharacter (`$(`, backtick, `;`, or whitespace) inside the
  brackets, which no valid comma-separated extras list contains
- Pattern library v1 -- review for environment-specific tuning before
  deployment; this rule requires application-level logging of the
  constructed pip install command string, which most infrastructure
  log sources do not capture by default
level: high
tags:
- attack.t1059
- wrg.observed.cve.cve_2026_16796
- wrg.observed.source.bedrock_agentcore_ghsa_j6g5_3hh3_pgw8
- wrg.observed.vector.extras_group_regex_too_permissive
- wrg.observed.vector.missing_shell_quoting
- wrg.severity.high
- wrg.observed
- wrg.tactic.execution