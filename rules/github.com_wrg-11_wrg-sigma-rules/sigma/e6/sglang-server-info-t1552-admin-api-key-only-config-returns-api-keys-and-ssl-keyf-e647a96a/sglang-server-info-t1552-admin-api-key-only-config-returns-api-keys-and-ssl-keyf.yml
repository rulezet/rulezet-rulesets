title: SGLang /server_info -- T1552 admin-api-key-only config returns API keys and SSL keyfile paths
id: 493339cf-96e6-4648-80e9-dfc0fbaf1ea5
status: experimental
description: |
  Vendor-disclosed detection for CVE-2026-15977 (GHSA-jx7q-p32r-7wx8,
  CVSS 7.5 HIGH), affecting SGLang.

  The `/server_info` diagnostic endpoint returns API keys and SSL
  keyfile information in its response when a deployment is configured
  with ONLY `--admin-api-key` set (a common configuration for
  admin-gated deployments that otherwise expect the regular inference
  API to remain open or separately secured). The endpoint's information
  disclosure was not gated on the same admin credential it was meant to
  protect, so a caller who can reach `/server_info` at all -- not
  necessarily one holding the admin key -- receives secrets that should
  never leave the server process.

  Detection logic flags a response from `/server_info` whose body
  contains fields recognizable as API-key or keyfile-path material
  -- the exact leak surface this advisory describes, distinct from the
  endpoint's intended (non-secret) diagnostic content.

  Aggregated from 2 independent sources (SGLang's own GHSA advisory + an
  independent security researcher's corroborating disclosure write-up,
  part of the same coordinated multi-CVE SGLang research series as the
  sibling execution rules above), no patched version confirmed in the
  summary available at authoring time.
references:
- https://github.com/sgl-project/sglang/security/advisories/GHSA-jx7q-p32r-7wx8
- https://thoughts.apoorvdayal.com/posts/sglang-disclosures/
- https://attack.mitre.org/techniques/T1552/
author: WinstonRedGuard -- sigma plugin observed rules (derived from breach corpus)
date: '2026-08-11'
logsource:
  category: proxy
  product: ""
detection:
  selection_server_info_endpoint:
    cs-uri-stem|contains: /server_info
  selection_method_get:
    cs-method: GET
  condition: selection_server_info_endpoint and selection_method_get
falsepositives:
- A deployment already upgraded to a fixed version once one ships, where
  `/server_info` no longer returns API-key/keyfile fields regardless of
  configuration -- a matching request on a fixed deployment receives
  only non-secret diagnostic content
- A deployment NOT configured with `--admin-api-key` alone (e.g. no auth
  configured at all, or a different auth combination) -- this rule
  cannot distinguish the specific vulnerable configuration from a
  request pattern alone; every `/server_info` request is flagged since
  network proxy logs typically cannot inspect response body content or
  server configuration
- Pattern library v1 -- review for environment-specific tuning before
  deployment
level: high
tags:
- attack.t1552
- wrg.observed.cve.cve_2026_15977
- wrg.observed.source.sglang_ghsa_jx7q_p32r_7wx8
- wrg.observed.source.apoorvdayal_disclosure_series
- wrg.observed.vector.diagnostic_endpoint_secret_leak
- wrg.observed.cluster.sglang_2026_08_disclosure_series
- wrg.severity.high
- wrg.observed
- wrg.tactic.credential_access