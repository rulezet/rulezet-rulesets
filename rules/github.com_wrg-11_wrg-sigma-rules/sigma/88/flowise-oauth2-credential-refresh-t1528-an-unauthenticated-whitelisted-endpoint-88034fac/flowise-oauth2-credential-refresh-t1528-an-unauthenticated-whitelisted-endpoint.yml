title: Flowise OAuth2 credential refresh -- T1528 an unauthenticated, whitelisted endpoint returns the live access_token in its response body
id: f7ffd0c7-6505-43ca-9cc6-c5e5100400f9
status: experimental
description: |
  Vendor-disclosed detection for CVE-2026-70478 (GHSA-qgvm-j2hm-6m38),
  affecting Flowise's OAuth2 credential refresh endpoint.

  `POST /api/v1/oauth2-credential/refresh/:credentialId` is listed in
  `WHITELIST_URLS`, so it requires no authentication at all. It
  decrypts the stored credential (`clientId`, `clientSecret`,
  `refresh_token`), exchanges the refresh token with the configured
  OAuth provider, and returns the new `access_token` DIRECTLY in the
  JSON response body. An attacker who obtains any credential ID --
  through a public chatflow leak, enumeration, or an unrelated IDOR --
  can call this endpoint with no session and no API key, and receive a
  live, usable access token for whatever third-party service (Google,
  Microsoft, GitHub) the victim credential is connected to.

  Detection logic flags a request to this refresh endpoint carrying no
  `Authorization` header and no session cookie -- the exact
  unauthenticated-reachability this advisory's Docker validation
  confirms (`Credential not found`, not `401 Unauthorized`, proving the
  endpoint processes the request before any auth check).

  Aggregated from 1 first-party vendor security advisory (FlowiseAI, via
  GHSA, citing the exact route file, line numbers, and a Docker-validated
  proof distinguishing "credential not found" from "unauthorized"), fixed in Flowise 3.1.3 (verified via live GHSA lookup, 2026-09-04 -- the rule's original authoring predates the vendor's patch release; this note corrects a stale claim, not the detection logic itself).
references:
- https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-qgvm-j2hm-6m38
- https://attack.mitre.org/techniques/T1528/
author: WinstonRedGuard -- sigma plugin observed rules (derived from breach corpus)
date: '2026-08-11'
logsource:
  category: webserver
  product: ""
detection:
  selection_refresh_endpoint:
    cs-uri-stem|contains: /api/v1/oauth2-credential/refresh/
  selection_post_method:
    cs-method: POST
  condition: selection_refresh_endpoint and selection_post_method
falsepositives:
- A deployment already upgraded past the fix, where the endpoint is
  removed from `WHITELIST_URLS` and requires authentication, or the
  response no longer includes `access_token` in the body -- a matching
  unauthenticated request on a fixed deployment is rejected before
  reaching the credential
- A legitimate internal health-check or monitoring probe that happens
  to hit this path pattern without credentials -- narrow given the
  specific `:credentialId` segment this endpoint requires to do
  anything meaningful
- Pattern library v1 -- review for environment-specific tuning before
  deployment; on a vulnerable deployment EVERY call to this endpoint
  is unauthenticated by construction (it is whitelisted from the auth
  middleware entirely), so this rule flags the endpoint itself rather
  than trying to distinguish authenticated from unauthenticated
  callers -- correlate with the absence of a valid session/API key in
  application-level logs to confirm exploitation rather than
  operator-initiated legitimate use
level: high
tags:
- attack.t1528
- wrg.observed.cve.cve_2026_70478
- wrg.observed.source.flowise_ghsa_qgvm_j2hm_6m38
- wrg.observed.vector.whitelisted_route_returns_secret
- wrg.observed.vector.token_in_response_body
- wrg.severity.high
- wrg.observed
- wrg.tactic.credential_access