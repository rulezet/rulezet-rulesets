import "pe"
rule EXEGUARD_v13_Hint_DOS_EP: PEiD
{
    strings:
        $a = { BA ?? ?? BF ?? ?? EB ?? E8 ?? ?? 7C ?? 79 ?? FA EA ?? ?? ?? ?? 76 ?? D8 FD }
    condition:
        $a at pe.entry_point

}