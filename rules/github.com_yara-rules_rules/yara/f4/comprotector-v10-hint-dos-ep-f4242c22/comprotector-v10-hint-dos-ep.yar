import "pe"
rule ComProtector_v10_Hint_DOS_EP: PEiD
{
    strings:
        $a = { E9 ?? ?? B9 ?? ?? BB ?? ?? BF ?? ?? 2E ?? ?? 34 ?? FE C8 C0 ?? ?? 2E ?? ?? 2E ?? ?? 2E ?? ?? 47 43 E2 }
    condition:
        $a at pe.entry_point

}