import "pe"
rule Encriptor_v100c_Hint_DOS_EP: PEiD
{
    strings:
        $a = { EB ?? B9 ?? ?? BE ?? ?? BF ?? ?? AC D0 C8 AA E2 ?? BE ?? ?? BF ?? ?? AC AA }
    condition:
        $a at pe.entry_point

}