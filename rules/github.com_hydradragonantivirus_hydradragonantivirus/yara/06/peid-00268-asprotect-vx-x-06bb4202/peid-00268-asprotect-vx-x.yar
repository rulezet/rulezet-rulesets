rule PEiD_00268_ASProtect_vx_x_ {
  meta:
    description = "[ASProtect vx.x]"
    ep_only     = "true"

  strings:
    $a = { 60 ?? ?? ?? ?? ?? 90 5D ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? 03 DD }

  condition:
    $a
}