import "pe"
rule PEiD_01944_REALbasic_
{
    meta:
        description = "[REALbasic]"
        ep_only = "true"
    strings:
        $a = {55 89 E5 ?? ?? ?? ?? ?? ?? ?? ?? ?? ?? 50 ?? ?? ?? ?? ?? 00}
    condition:
        $a at pe.entry_point
}