SecRule TX:DETECTION_PARANOIA_LEVEL "@ge 4"      "id:959155,     phase:3,     pass,     t:none,     nolog,     tag:'OWASP_CRS',     ver:'OWASP_CRS/4.26.0-dev',     setvar:'tx.detection_outbound_anomaly_score=+%{tx.outbound_anomaly_score_pl4}'"

# at start of phase 4, we reset the aggregate scores to 0 to prevent duplicate counting of per-PL scores
# this is necessary because the per-PL scores are counted across phases
SecAction      "id:959059,     phase:4,     pass,     t:none,     nolog,     tag:'OWASP_CRS',     ver:'OWASP_CRS/4.26.0-dev',     setvar:'tx.blocking_outbound_anomaly_score=0'"

SecAction      "id:959159,     phase:4,     pass,     t:none,     nolog,     tag:'OWASP_CRS',     ver:'OWASP_CRS/4.26.0-dev',     setvar:'tx.detection_outbound_anomaly_score=0'"

SecMarker "EARLY_BLOCKING_ANOMALY_SCORING"

# Summing up the blocking and detection anomaly scores in phase 4